rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public reads of images (products/certificates)
    // Only authenticated admin users can upload or delete them
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}